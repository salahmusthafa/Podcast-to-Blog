import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { QueryProvider } from '@/components/providers/QueryProvider'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Podcast to Blog - Convert Your Podcasts into Blog Posts',
  description: 'Transform your podcast episodes into SEO-optimized blog posts with AI-powered transcription and summarization.',
  keywords: 'podcast, blog, transcription, AI, content creation, SEO',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <QueryProvider>
          {children}
        </QueryProvider>
      </body>
    </html>
  )
} 